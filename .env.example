# OpenAI API key for LLM analysis (REQUIRED)
OPENAI_API_KEY=your_openai_api_key_here

# Backend API URL for the frontend to connect to (REQUIRED)
# This URL must be accessible from the browser, not just from the containers
# In development with Docker, this is typically http://localhost:8000
BACKEND_API_URL=http://localhost:8000

# Set to production in production environment
NODE_ENV=development 